<%= render :partial => 'spree/admin/shared/configuration_menu' %>

<div class="toolbar" data-hook="toolbar">
  <ul class="actions">
    <li>
      <p><%= button_link_to 'New Asset', new_object_url, :icon => 'add', :id => 'admin_new_asset_link' %></p>
    </li>
  </ul>
  <br class="clear" />
</div>

<h1>Assets</h1>

<table class="index" id="listing_slides" data-hook>
  <thead>
    <tr data-hook="admin_slides_index_headers">
      <th>Name</th>
      <th data-hook="admin_slides_index_header_position">URL</th>
      <th data-hook="admin_slides_index_header_actions"></th>
    </tr>
  </thead>
  <tbody>
    <% @custom_assets.each do |item|%>
      <tr id="<%= dom_id item %>" data-hook="admin_assets_index_rows">
        <td width="350px" ><%= item.name %></td>
        <td data-hook="admin_slides_index_position"><%= item.image.url %></td>
        <td data-hook="admin_slides_index_row_actions">
          <%= link_to_edit item, :class => 'edit' %> &nbsp;
          <%= link_to_delete item %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
